import React from "react";

const Topbar = () => {
  return (
    <header className="w-full bg-white shadow-sm h-14 flex items-center px-4 justify-between">
      <h1 className="text-xl font-semibold text-primary">Curiflow</h1>
    </header>
  );
};

export default Topbar;
