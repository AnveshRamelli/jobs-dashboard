const Topbar = () => {
  return (
    <header className="w-full bg-white shadow-sm h-14 flex items-center px-4 gap-2">
      <img src="/logo.png" alt="Logo"/>
      <h1 className="text-xl font-semibold text-primary">Jobflow</h1>
    </header>
  );
};

export default Topbar;
